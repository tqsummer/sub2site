# 仓库统一 LF。项目源码已规范化为 LF，若让 Git 在检出时转 CRLF，
# 每次 clone 都会产生全文件差异，diff 完全不可读。
* text=auto eol=lf

# 二进制文件不做任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
