# AIProxy 用户端重设计简报（交付 Claude Design）

## Context

`sub2site` 已完成从 sub2api 的复刻与管理端裁剪，功能完整可用，但视觉仍是 sub2api 原样（Teal 主色、终端动画首页、原布局）。现在要做**全新视觉设计**：功能一个不减，布局/样式/首页全部重做，让人看不出与 sub2api 的血缘。

已定方向：
- **交付目标**：Claude Design
- **视觉基调**：石墨黑 + 电光蓝，亮色为主 + 完整暗色双模
- **目标客户**：企业技术团队与 AI 应用开发者兼顾
- **公开页策略**：模型广场、密钥用量查询提升为主导航入口

本文档三部分：**① 功能全景**（存档与自查）、**② 主简报**（一次性贴给 Claude Design）、**③ 逐页提示词**（按需逐页贴）。② 和 ③ 是可直接复制的成品。

---

# 第一部分：功能全景（存档）

## 登录后功能页（13）

| 路径 | 名称 | 复杂度 | 核心内容 |
|---|---|---|---|
| `/dashboard` | 仪表盘 | 中 | 8 张指标卡 + 平台配额拆分 + 甜甜圈/折线双图 + 近期用量 + 快捷入口 |
| `/keys` | API 密钥 | **极高** | 13 列表格 + 6 个弹窗（含 1867 行的「使用密钥」代码生成器）+ 分组切换浮层 + 列设置 |
| `/usage` | 用量明细 | 高 | 4 张统计卡 + 4 张图表 + 双 Tab（用量/错误）+ 各 7/4 个筛选器 + CSV 导出 |
| `/subscriptions` | 我的订阅 | 低 | 订阅卡网格：日/周/月额度进度条 + 到期倒计时 + 续费 |
| `/purchase` | 充值/订阅 | **极高** | 双 Tab + 金额九宫格 + 支付方式品牌卡 + 套餐卡 + 支付状态机面板（二维码/deeplink/轮询） |
| `/orders` | 我的订单 | 低 | 订单表 + 取消/退款弹窗 |
| `/redeem` | 兑换码 | 低 | 余额横幅 + 兑换表单 + 结果卡 + 活动流水 |
| `/profile` | 个人资料 | 高 | Hero 概览 + 头像 + 密码 + 身份绑定(6 家) + 余额告警 + TOTP 向导 + Passkey |
| `/monitor` | 渠道状态 | **极高** | **两套 UI**：V1 状态页卡片风 / V2 运维仪表盘（KPI + 脉冲热力矩阵 + 三 Tab 明细） |
| `/available-channels` | 可用渠道 | 中 | 渠道×平台×分组×模型 的 rowspan 合并表 |
| `/batch-image` | 批量图片生成 | **极高** | 父子行表格 + 4 个弹窗 + Prompt 构建器 + ZIP 下载 |
| `/affiliate` | 推广返利 | 低 | 4 张统计卡 + 邀请码/链接复制 + 额度转入 + 被邀请人表 |
| `/custom/:id` | 自定义页 | 中 | Markdown（带 TOC 侧栏）或 iframe 两种模式 |

## 公开页（10）

| 路径 | 名称 | 说明 |
|---|---|---|
| `/home` | 落地页 | **重设计核心**。现为 hero + 终端动画 + 3 特性 + 5 平台徽章 |
| `/model-plaza` | 模型广场 | 公开价格表：分组 × 模型 × 实付/官方价 × 折扣倍率，四级联动筛选 |
| `/key-usage` | 密钥用量自助查询 | 免登录，粘贴 Key 查额度。圆环图 + 日/模型明细表 |
| `/login` `/register` | 登录/注册 | 表单 + 6 家 OAuth + 验证码 + 协议弹窗 + 2FA |
| `/forgot-password` `/reset-password` `/email-verify` | 密码与验证 | 多态卡片 |
| `/legal/:id` | 法律文档 | Markdown 渲染 |
| `/payment/result` 等 4 页 | 支付回跳 | 状态机 + 轮询 |

## 全站共用能力

**数据展示**：13 列级表格（桌面 table / 移动卡片双模式、虚拟滚动、sticky 表头与操作列、列设置持久化）、甜甜圈图、堆叠折线图、热力脉冲矩阵、圆环进度、多档进度条（阈值三色）、状态徽章。

**交互模式**：5 档宽度模态框、确认对话框、移动端抽屉、下拉菜单、Teleport 浮层、Popover/Tooltip、Toast、分页器、日期范围选择器（8 预设）、可搜索/可创建下拉、Toggle、Tab（药丸式与下划线式）、复制到剪贴板（绿勾反馈）、文件上传/下载、自动刷新倒计时、手风琴、新手引导 Tour。

**贯穿全站的信息设计惯例**（必须保留可辨识性）：
- **双值成本**：实际成本（彩色高亮）/ 标准成本（灰色删除线）
- **倍率对比**：专属倍率加粗、原倍率删除线、高峰倍率独立琥珀 chip
- **配额三窗口**：日/周/月，各带进度条与重置倒计时；未配置时显示「∞ 无限制」
- **平台色彩编码**：Claude / OpenAI / Gemini / Antigravity / Grok 各有固定识别色

---

# 第二部分：主简报（直接复制给 Claude Design）

> 以下整段复制。它会建立设计系统、全局布局和首页，是后续所有页面的基准。

```
我需要为 AIProxy 设计一套完整的 Web 产品界面。这是一个面向企业与开发者的 AI 模型 API 网关平台。

## 产品是什么

用户在这里获得一个 API Key，用它调用 Claude、OpenAI、Gemini、Antigravity、Grok 等各家大模型，无需分别申请账号。平台负责多账号智能调度、负载均衡、用量计量和成本控制。两种计费模式并存：按量计费（充值余额，按 token 扣费）和订阅套餐（固定月费，额度内畅用）。

## 目标用户（双重）

1. **企业技术团队**：关心成本可控、稳定性、团队配额管理、发票合规。他们看的是仪表盘上的钱和用量。
2. **AI 应用开发者**：关心接入速度、协议兼容、代码示例。他们看的是 API Key 页和文档。

界面要同时服务这两类人：数据密集、专业、可信，但接入路径要短。

## 视觉方向

**基调：石墨黑 + 电光蓝。** 克制、精确、有基础设施感——参照 Stripe 和 Cloudflare 的气质，而不是消费级 AI 产品的炫彩渐变。

- **中性色是骨架**：大面积使用石墨灰阶构建层次，界面的 90% 由中性色承载
- **电光蓝 #2563EB 是唯一强调色**：只用在主行动按钮、当前选中态、关键数据高亮、链接。绝不大面积铺开
- **语义色克制使用**：成功绿、警告琥珀、危险红，仅用于状态表达，不参与装饰
- **禁止**：紫粉渐变、发光光晕、玻璃拟态、赛博朋克霓虹、AI 产品常见的紫色渐变按钮

**双模交付**：亮色为主（面向企业客户的默认），同时提供完整的暗色方案（面向开发者）。两套都要设计，不是简单反色——暗色下要重新平衡对比度和层次，深色背景用石墨黑而非纯黑。

## 设计系统要求

**字体**
- 界面字体：现代无衬线（Inter 或同类），中文需良好回退
- 数据与代码：等宽字体（JetBrains Mono 或同类）
- 所有数字使用 tabular-nums 对齐——这是数据密集型产品，列表里的数字必须能竖向对齐比较

**密度**
这是专业工具，不是营销页。信息密度要高：表格行高紧凑、卡片内边距适中、避免大留白浪费视口。但要靠清晰的层次和分组避免拥挤感。

**圆角**
中等圆角（按钮和输入框 8px，卡片 12px，弹窗 16px）。避免过度圆润带来的消费级感。

**阴影**
极克制。卡片用极淡的边框而非阴影来分层；只有浮层（下拉、弹窗、Popover）用阴影表达层级。禁止彩色阴影和发光效果。

**动效**
功能性动效为主：状态切换 150ms，浮层出现 200ms，页面过渡不超过 300ms。禁止装饰性动画、视差滚动、入场动画序列。

## 站点结构

**公开区**（未登录可访问）
- 落地页 `/`
- 模型广场 `/model-plaza` —— 公开价格表，客户不注册就能比价
- 密钥用量查询 `/key-usage` —— 免登录自助工具，粘贴 API Key 查额度
- 登录 / 注册 / 找回密码 / 邮箱验证
- 法律文档

**控制台**（登录后，侧边栏导航）
- 仪表盘 —— 余额、用量、成本总览
- API 密钥 —— 创建和管理 Key
- 用量明细 —— 逐条调用记录与图表分析
- 渠道状态 —— 服务可用性监控
- 可用渠道 —— 能访问哪些平台和模型
- 我的订阅 —— 订阅套餐额度与到期
- 充值 / 订阅 —— 购买
- 我的订单
- 兑换码
- 推广返利
- 批量图片生成
- 个人资料

## 全局布局

**控制台外壳**
- 左侧固定侧边栏：展开 260px / 折叠 72px（只留图标），底部有折叠开关和明暗切换。小于 1024px 变为抽屉式（汉堡菜单唤出，带遮罩）
- 顶栏 sticky：左侧显示当前页面标题与描述；右侧依次为 公告铃铛（未读红点）、文档外链、模型广场入口、语言切换（中/英）、订阅进度胶囊、**余额胶囊**（hover 展开「可用/冻结/合计」三行卡片）、用户头像下拉菜单
- 主内容区自适应侧边栏宽度

**余额胶囊是高频信息**，要设计得醒目但不喧宾夺主——用户随时想知道自己还有多少钱。

**公开页外壳**
顶部导航条：Logo + 站名 | 模型广场 · 密钥查询 · 文档 | 语言切换 · 明暗切换 · 登录/控制台按钮

## 落地页要求

这是重设计的重点，必须完全重做，不要沿用任何现有结构。

**叙事顺序**（企业叙事在前，开发者路径紧随）：
1. **首屏**：一句话价值主张 + 副标题 + 双 CTA（免费开始 / 查看价格）。右侧或下方配一个能表达「多模型统一接入」的视觉——但不要用终端窗口模拟器
2. **信任层**：支持的模型厂商标识墙（Claude / OpenAI / Gemini / Antigravity / Grok）
3. **两种计费模式对比**：按量计费 vs 订阅套餐，讲清楚区别和适用场景。这是转化关键
4. **核心能力三到四点**：统一接入 · 智能调度与故障转移 · 用量与成本可见 · 团队配额管理
5. **开发者接入路径**：三步接入 + 一段真实可复制的代码示例（curl 或 SDK），强调兼容 OpenAI/Anthropic 协议
6. **价格透明入口**：引导到模型广场看完整价格表
7. **底部 CTA + 页脚**

**禁止出现**：终端窗口动画、"订阅转 API"这类内部黑话、GitHub 开源仓库链接、版本号。

## 明确的禁止项（重要）

这个产品的当前版本基于一套开源方案，重设计必须彻底摆脱它的痕迹：
- 不使用青绿色（Teal #14b8a6）作为主色或强调色
- 不使用终端/命令行窗口模拟器作为首页主视觉
- 不使用玻璃拟态卡片 + 彩色模糊光斑背景的组合
- 不在界面任何位置显示后端版本号
- 页脚不放开源仓库链接

## 交付内容

请先产出：
1. **设计系统**：完整色板（亮色与暗色两套，含中性 10 级色阶、主色、语义色）、字体阶梯、间距系统、圆角与阴影规范、动效时长
2. **核心组件**：按钮（5 种变体）、输入框、下拉选择、开关、徽章、卡片、模态框、Toast、表格行、进度条、Tab、分页器
3. **落地页**：完整设计，亮色 + 暗色
4. **控制台外壳**：侧边栏（展开/折叠/移动抽屉三态）+ 顶栏（含余额胶囊展开态和用户下拉展开态）
5. **仪表盘页**：作为控制台的视觉基准

后续我会逐页提供其余页面的详细规格。
```

---

# 第三部分：逐页提示词（按需复制）

> 主简报产出设计系统后，逐页贴。每段独立完整，可单独使用。

## 仪表盘 `/dashboard`

```
设计 AIProxy 控制台的仪表盘页，沿用已建立的设计系统。

页面目的：一屏看清账户余额、Key 数量、今日与累计消耗、各平台配额、近期请求。

区块自上而下：

1. 指标卡组（第一行 4 张）：账户余额（可用金额）· API 密钥（总数 + 活跃数）· 今日请求（+ 累计总数）· 今日消费（实际成本高亮显示，标准成本以灰色小字对照）

2. 指标卡组（第二行 4 张）：今日 Tokens（总量 + 输入/输出/缓存三项拆分）· 累计 Tokens（同结构）· 性能（RPM 与 TPM 双值）· 平均响应耗时

3. 平台拆分卡：一张大卡内含 2-4 列小卡，每个 AI 平台一个（Claude/OpenAI/Gemini/Antigravity/Grok，各有固定识别色）。每个小卡显示：平台名 + 累计花费，下方三行明细（今日消费/请求数/Tokens），再下方是配额进度区——日/周/月三个窗口各一条进度条，格式为「已用 $X / 上限 $Y」+ 进度条 + 「N 小时后重置」。进度条按用量三色分级（<75% 正常、75-95% 警告、≥95% 危险）。若某平台未配置配额则不显示该区。当各平台之和小于总值时，末尾补一张虚线边框的「其他」卡

4. 图表区（两列）：左为模型分布甜甜圈图 + 右侧可滚动明细表（模型/请求数/Tokens/实际/标准）；右为 Token 使用趋势堆叠折线图（输入/输出/缓存创建/缓存读取/缓存命中率五条线）。图表区上方有一行控制条：日期范围选择器 + 刷新按钮 + 粒度切换（按天/按小时）

5. 底部两栏（左 2/3 右 1/3）：左为「最近用量」列表（最多 5 条，每条含模型名、时间、实际花费、标准花费、token 合计），底部「查看全部」链接；右为「快捷操作」，4 个大按钮行（创建 API Key / 查看用量 / 批量图片 / 兑换码），每行含图标、主标题、副描述、右侧箭头

请同时设计：整页加载骨架态、图表无数据的空态、最近用量列表的空态。
响应式：指标卡在移动端 2 列、桌面 4 列；图表移动端单列堆叠。

亮色与暗色两套。
```

## API 密钥 `/keys`

```
设计 AIProxy 控制台的 API 密钥管理页。这是全站最复杂的页面之一。

页面目的：创建、配置、监控 API Key，并提供各类客户端的接入配置代码。

【主界面】

顶部筛选行：搜索框 + 分组下拉 + 状态下拉（全部/活跃/停用/配额耗尽/已过期）
端点条：横向排列的可用 API 端点 chip，每个含端点名、「默认」标记、可点击复制的 URL，hover 显示描述气泡
操作行（右对齐）：刷新 · 列设置下拉（复选清单）· 创建密钥（主按钮）

数据表格（13 列，列可显隐且持久化）：
名称（配了 IP 限制时附盾牌图标）· ID · API Key（掩码显示 + 复制按钮，复制后图标变绿勾）· 分组（显示平台图标+分组名+倍率，整块可点击打开分组切换浮层）· 当前并发 · 用量（今日/累计金额，配了配额则加一行进度条）· 速率限制（5小时/1天/7天三段，各含已用/上限+细进度条+重置时间）· 过期时间 · 状态徽章 · 最后使用 · 最后使用 IP · 创建时间 · 操作

每行操作是 5 个「图标在上文字在下」的竖排小按钮：使用密钥 · 导入客户端 · 启用/停用 · 编辑 · 删除

【弹窗 1：创建/编辑密钥】
字段：名称 · 分组（可搜索下拉，选项显示平台图标+名称+倍率+描述）· 自定义 Key（开关展开）· 状态（仅编辑时）· IP 限制（开关展开为白名单/黑名单双 textarea）· 配额上限（$ 前缀数字框，0 为无限；编辑时附「已用 $X / $Y」和重置按钮）· 速率限制（开关展开为三个数字框）· 有效期（开关展开为 7/30/90 天快捷按钮 + 自定义日期）

【弹窗 2：使用密钥】—— 最复杂的弹窗
这是一个多维度的配置代码生成器：
- 客户端 Tab 行（横向可滚动，按所选平台动态变化：Claude Code / Codex CLI / Gemini CLI / OpenCode / Grok CLI 等）
- 操作系统 Tab 行（Unix / Windows / CMD / PowerShell）
- 代码块列表（可多个文件堆叠）：每块是深色代码卡，含文件路径标题栏 + 复制按钮 + 语法高亮内容
- 部分代码块上方有提示条
- 底部有蓝色使用说明框

【浮层：分组切换器】
从表格内点击分组单元格唤出，含搜索框 + 分组列表（每项显示平台图标、名称、倍率、描述），当前分组高亮

请同时设计：表格空态（引导创建第一个密钥）、加载骨架、移动端表格转卡片的形态（每行变一张卡，字段标签在左值在右，操作区在底部）。

亮色与暗色两套。
```

## 用量明细 `/usage`

```
设计 AIProxy 控制台的用量明细页。

页面目的：多维筛选查看逐条 API 调用记录、错误请求，以及图表分析，支持 CSV 导出。

区块：

1. 统计卡组（4 张）：总请求数 · 总 Tokens（含输入/输出/缓存拆分，缓存项 hover 展开「缓存创建」「缓存读取」两行气泡）· 总花费（实际花费高亮，标准花费带删除线灰色对照）· 平均耗时

2. 时间控制卡：日期范围选择器（预设含今天/昨天/近24小时/近7天/近14天/近30天/本月/上月 + 自定义）+ 粒度选择（天/小时）

3. 四张图表（2×2）：模型分布甜甜圈 + 明细表 · 分组分布甜甜圈 + 明细表 · 端点分布甜甜圈 + 明细表 · Token 趋势折线。甜甜圈图右侧的明细表可点击行展开下钻子表

4. Tab 切换：「用量记录」与「错误请求」

5. 筛选卡（两个 Tab 各一套）：
   用量 Tab：7 个下拉——API Key、模型（可搜索）、分组（可搜索）、请求类型、压缩过滤、计费类型、计费模式
   错误 Tab：4 个下拉——Key 名、模型（可搜索且允许自由输入）、错误分类、状态码
   右侧按钮：刷新 · 重置 · 列设置 · 导出 CSV

6. 数据表格（列可显隐持久化）：
   用量表：API Key / 模型 / 推理强度 / 端点 / IP / 分组 / 类型 / 计费模式 / Tokens / 花费 / 延迟 / 时间 / User-Agent
   错误表：Key 名 / 模型 / 端点 / IP（附归属地）/ 分组 / 类型 / 平台 / 错误分类 / 状态码（按码值配色）/ 错误消息（截断）/ 时间。整行可点击打开错误详情弹窗

【弹窗：错误详情】两列网格显示时间、模型、端点、状态码徽章、分类、平台、上游状态码，下方是错误消息和响应体（等宽字体、可滚动区）

请设计：CSV 导出进行中的按钮态、筛选切换时的加载态、表格空态。

亮色与暗色两套。
```

## 我的订阅 `/subscriptions`

```
设计 AIProxy 控制台的订阅管理页。

页面目的：查看已购订阅套餐的额度消耗与到期时间，一键续费。

布局：卡片网格（桌面两列，移动单列）。每张订阅卡：

卡头：左侧是平台色圆点 + 分组名 + 平台徽章 + 分组描述 + 元信息行（计费倍率、高峰时段倍率及其时间窗口）；右侧是状态徽章（生效/已过期）+ 「立即续费」按钮

卡身：
- 到期信息行：「剩余 N 天（具体日期时间）」，按紧急度分级配色（已过期红 / ≤3天红 / ≤7天橙 / 其他中性）
- 日额度：「已用 $X / 上限 $Y」+ 进度条 + 「N 小时 M 分后重置」
- 周额度：同结构
- 月额度：同结构
- 进度条按用量三色分级（≥90% 危险 / ≥70% 警告 / 其他正常）
- 若该订阅未配置任何额度限制，则整个额度区替换为一个「无限制」的视觉块（用 ∞ 符号，需要有分量感但不能俗气）

空态：居中卡片，说明暂无有效订阅。

亮色与暗色两套。
```

## 充值与订阅购买 `/purchase`

```
设计 AIProxy 控制台的充值/订阅购买页。这是转化关键页，也是交互最复杂的页面之一。

页面有两大阶段：选择阶段 和 支付进行中阶段。

【选择阶段】

顶部 Tab 切换：「充值」与「订阅」

充值 Tab：
1. 充值账户卡：用户名 + 当前余额
2. 金额输入卡：「快捷金额」九宫格按钮（10/20/50/100/200/500/1000/2000/5000），选中态高亮；下方「自定义金额」输入框（$ 前缀，显示可用范围）
3. 支付方式选择器：网格卡片，每个含支付品牌图标 + 名称 + 手续费百分比。选中态使用各支付品牌自身的识别色作为边框。不可用的置灰
4. 费用明细卡：支付金额 / 手续费 / 实付（大号强调）/ 到账余额
5. 全宽主提交按钮，按所选支付方式着色

订阅 Tab：
1. 套餐卡网格（1个居中/2个双列/3个及以上三列）。每张套餐卡：顶部平台色装饰条 + 套餐名 + 描述 + 右侧大号价格（含原价删除线和折扣徽章）+ 有效期 + 配额区（倍率/高峰倍率/日周月限额/模型范围标签）+ 特性勾选列表。hover 上浮
2. 下方「当前订阅」紧凑列表
3. 选中套餐后进入确认态：套餐详情卡 + 支付方式选择器 + 费用明细 + 创建订单按钮

【支付进行中阶段】—— 一个状态机面板，需要设计以下所有状态：
- 二维码支付：扫码区（二维码中心叠加支付品牌 Logo 圆章）+ 金额 + 订单号 + 倒计时（mm:ss 大号等宽数字）+ 「等待支付」提示
- 移动端支付宝唤起：正在打开 → 已切至后台（含「重新打开」按钮）→ 超时降级为二维码
- 支付成功：绿色勾 + 订单信息卡 + 确认按钮
- 已取消：中性图标 + 说明 + 确认
- 已过期/失败：警告图标 + 说明 + 确认

请设计：套餐卡的 hover 态、支付方式的选中态、倒计时的紧迫感表达。

亮色与暗色两套。
```

## 渠道状态 `/monitor`

```
设计 AIProxy 控制台的渠道状态监控页。这个页面有两套完全不同的形态，请分别设计。

【形态 A：服务状态页风格】

顶部工具条（右对齐）：时间窗口 Tab（7天/15天/30天）+ 整体状态胶囊（呼吸圆点 + OPERATIONAL 或 DEGRADED）+ 手动刷新 + 自动刷新下拉（含开关和 30s/60s/120s 间隔选项，开启时按钮显示倒计时）

主体：监控卡网格。每张卡：
- 卡头：厂商图标块 + 渠道名 + 平台徽章 + 主模型（等宽字体）+ 分组徽章 + 右侧状态胶囊
- 指标对：主指标「延迟 XXms」+ 副指标「端点 Ping XXms」
- 配额快照区（若为配额模式）：套餐等级徽章 + 多条用量进度条 + 余额
- 可用率行：「可用率 · 7d」+ 百分比 + 「另有 N 个模型」
- 时间线：分桶状态条带（类似 status page 的可用性条）

点击卡片打开详情弹窗：表格显示各模型的最新状态、延迟、7/15/30 天可用率、平均延迟。

【形态 B：运维仪表盘风格】

顶部粘性头部卡：标题 + 状态行（呼吸点 + 「数据截止 HH:MM」+ 可能的「数据不完整」警告徽章 + 「回填中 X%」进度徽章）+ 刷新按钮
下方单行紧凑工具条（横向可滚动）：时间范围 Tab（90分钟/24小时/7天/30天）| 平台多选 · 分组多选 · 模型多选 · 清除筛选 | 矩阵分组方式下拉 | 趋势视图 Tab（脉冲矩阵/折线）| 健康模式 Tab（综合/成功率/首字延迟/缓存）

KPI 卡组（4-5 张）：成功率（+错误率副值）· 首字延迟 P50（+P90/P99 副值）· Tokens/秒 · 缓存命中率 · RPM。每张卡左侧有健康状态圆点（良好/警告/异常三色）

趋势可视化区（二选一）：
- 折线视图：三条线（错误率/缓存率/首字延迟），支持滚轮横向缩放，附「重置缩放」按钮
- 脉冲矩阵：热力网格，表头为 维度|成功率|首字延迟|TPS|缓存率|脉冲时间轴，每行的时间轴由按健康度着色的色块组成，支持缩放

明细 Tab 卡（三个 Tab）：
- 模型表：平台+模型（带健康圆点）/ 成功率 / 首字延迟 / TPS / 缓存率 / RPM，行可点击下钻
- 错误列表：手风琴式，每条含分类名 + 横向占比条 + 百分比，展开显示明细
- 用户排行表：排名徽章 / 用户（当前用户行高亮并加标记）/ 各项指标

请设计：KPI 卡加载骨架、「正在回填数据」与「暂无数据」两套空态、脉冲矩阵的色阶图例。

亮色与暗色两套。
```

## 个人资料 `/profile`

```
设计 AIProxy 控制台的个人资料页。内容较多，需要清晰的分区。

区块自上而下：

1. Hero 概览卡：大号圆角头像 + 用户名 + 角色徽章 + 账号状态徽章 + 主邮箱 + 身份来源标记；右侧或下方是三个指标（账户余额 / 并发限制 / 注册时间）

2. 基础资料面板（两列）：左为头像卡（预览 + 上传/保存/删除三按钮）；右为资料编辑（用户名输入 + 更新按钮）

3. 身份绑定面板：每个身份提供商一行（邮箱、Google、LinuxDo、钉钉、微信、OIDC）。每行含图标块 + 提供商名 + 绑定状态徽章 + 摘要信息 + 右侧操作按钮（绑定/解绑/管理）。邮箱行点击「管理」后展开内联表单：邮箱输入 + 发送验证码按钮 + 6 位验证码输入 + 密码 + 提交

4. 修改密码卡：当前密码 / 新密码 / 确认新密码 + 提交按钮

5. 余额告警卡：总开关，开启后展开——阈值设置（$ 输入 + 保存）+ 额外通知邮箱管理（已保存条目行含独立开关、验证状态、验证/移除操作；未验证时内联展开验证码流程；底部新增邮箱输入行）

6. 两步验证卡：四种状态（加载中 / 功能未启用 / 已启用含启用时间和关闭按钮 / 未启用含启用按钮）
   配套弹窗是三步向导：身份验证（邮箱验证码或密码）→ 二维码展示（含手动输入的密钥和复制按钮）→ 6 位验证码输入（6 个独立字符格，自动跳格）

7. Passkey 卡：列表展示已注册凭据（名称、同步状态徽章、创建与最后使用时间、编辑/删除操作）+ 添加表单（名称 + 当前密码）+ 空态

请设计：6 位验证码输入格的聚焦态、内联展开表单的过渡、各卡片的分区层次。

亮色与暗色两套。
```

## 落地页 `/home`（重点）

```
设计 AIProxy 的落地页。这是产品门面，需要同时说服企业采购者和技术开发者。

产品一句话：一个 API Key，接入 Claude、OpenAI、Gemini、Antigravity、Grok 全系模型，用量与成本完全可见。

页面结构：

1. 顶部导航：Logo + 站名 | 模型广场 · 密钥查询 · 文档 | 语言切换 · 明暗切换 · 登录按钮

2. 首屏：
   - 主标题：一句话价值主张
   - 副标题：说明多模型统一接入 + 成本可控
   - 双 CTA：「免费开始」（主按钮）和「查看价格」（次按钮，指向模型广场）
   - 主视觉：需要表达「多个模型厂商 → 统一入口 → 你的应用」这个概念。不要用终端窗口模拟器，考虑用抽象的架构示意、数据流动的几何图形，或一个简洁的调度可视化

3. 信任层：支持的模型厂商标识墙，五家（Claude / OpenAI / Gemini / Antigravity / Grok）

4. 两种计费模式对比（转化关键）：并列两栏
   - 按量计费：充值余额，按实际 token 消耗扣费，无月费门槛，适合用量波动或初期试用
   - 订阅套餐：固定月费，额度内不再逐笔计费，适合用量稳定的长期高频场景
   需要清楚表达「两者独立、互不冲突、可按需选择」

5. 核心能力（三到四张卡）：
   - 统一接入：一个 Key 调用所有模型，兼容 OpenAI 与 Anthropic 协议
   - 智能调度：多账号池自动切换与负载均衡，规避限流
   - 成本可见：实时用量统计、多维度分析、配额上限控制
   - 团队管理：多 Key 分组管理、独立配额、用量归因

6. 开发者接入路径：三步（注册获取 Key → 替换 base_url → 开始调用）+ 一段真实可复制的代码示例（curl 或 Python SDK），配复制按钮

7. 价格透明区：引导到模型广场，强调「不注册也能查完整价格」

8. 底部 CTA 区 + 页脚（版权、文档、法律条款、联系方式）

禁止：终端窗口动画、玻璃拟态卡片配彩色光斑背景、开源仓库链接、版本号、"订阅转 API"这类内部术语。

亮色与暗色两套。移动端完整适配。
```

## 模型广场 `/model-plaza`

```
设计 AIProxy 的模型广场页——公开的价格透明页，未登录也能访问，是重要的转化工具。

页面目的：按分组维度陈列所有可用模型与价格，让客户在注册前就能比价。

结构：

1. 顶部导航条（公开页样式，含登录/返回控制台按钮）

2. 页头：标题 + 说明

3. 全局价格说明区（管理员配置的富文本内容）

4. 未登录提示条：说明登录后可查看专属分组与专属倍率

5. 四级联动筛选栏：平台筛选 · 分组筛选 · 倍率筛选 · 模型名搜索。三个维度互相约束——在当前其他筛选条件下无结果的选项要置灰禁用而非隐藏，让用户理解筛选逻辑

6. 分组区块列表：每个分组一张卡
   - 卡头：分组徽章（名称+平台+倍率）+ 可能的「专属分组」标记 + 「订阅制」标记
   - 分组描述
   - 高峰时段提示条（若配置）：「高峰时段 20:00-24:00 计费倍率 ×2」
   - 模型价格表

7. 模型价格表（关键设计挑战）：双层表头的宽表
   - 第一层：模型 | 实付价格（折后）横跨三列 | 官方价格 横跨三列 | 折扣倍率
   - 第二层：输入 / 输出 / 缓存
   - 「实付价格」区需要视觉强调（这是卖点），「官方价格」区弱化处理作为对照
   - 模型名可附徽章：分时时段、跨平台标记、非 token 计费模式（按次/按图片）
   - 阶梯计价时每档一行
   - 表格需横向滚动，数字用等宽对齐

空态：无匹配结果时的提示。

亮色与暗色两套。这个页面的表格在移动端如何呈现需要专门设计。
```

## 密钥用量查询 `/key-usage`

```
设计 AIProxy 的密钥用量自助查询页——免登录工具，用户粘贴自己的 API Key 即可查询额度与用量。

这是一个对开发者友好的工具页，也是产品透明度的体现。

结构：

1. 顶部导航条（公开页样式，无登录按钮，只有语言和明暗切换）

2. 页头：标题「API Key 用量查询」+ 副标题

3. 查询区（居中，最大宽度约 600px）：
   - 输入行：带锁图标的大号输入框（placeholder 为 key 格式示例，右侧有明文切换眼睛图标）+ 查询按钮
   - 下方隐私说明小字：「您的 Key 仅在浏览器本地处理，不会被存储」——这句话要让人安心
   - 查询成功后出现日期范围选择器（今日/7天/30天/自定义四个胶囊）

4. 结果区：
   - 状态徽章（居中胶囊）：呼吸圆点 + 模式名 + 状态文字
   - 圆环进度卡网格（1-3 张，按数据自适应）：每张含标题 + 右上图标 + 大号 SVG 圆环（百分比数字在环心，下方是「已用 / 上限」金额和重置时间）。圆环需要入场动画和数字递增动画
   - 详细信息卡：分隔线列表，每行是图标块 + 标签 + 右侧数值（数值按用量比例分级着色）
   - Token 统计网格：今日与累计的请求数、输入、输出、缓存创建、缓存读取、费用、RPM/TPM、平均耗时
   - 按日明细表（含 7/30/90 天分段切换）：日期/请求数/输入/输出/缓存读取/缓存写入/费用
   - 模型用量表：模型/请求数/输入/输出/缓存创建/缓存读取/总 Tokens/费用

请设计：查询中的骨架态（圆环占位）、查询失败的处理、圆环的色阶方案。

亮色与暗色两套。
```

## 登录与注册

```
设计 AIProxy 的登录页与注册页，共用一套认证布局。

【认证布局外壳】
居中卡片式布局。上方是品牌区（Logo + 站名 + 副标题），中间是内容卡，下方是切换链接和版权。
重要：需要在这个布局上提供语言切换和明暗切换（当前版本缺失，是个体验缺口）。

【登录页】
- 标题 + 副标题
- 邮箱输入（左内嵌图标）
- 密码输入（左内嵌图标 + 右侧明文切换）+ 右对齐的「忘记密码」链接
- 人机验证区（需要为三种验证码方案预留位置：内联嵌入式 widget，以及点击提交才弹出的动作触发式）
- 登录协议区：两种形态——复选框式（一行勾选 + 内联文档链接）和弹窗式（页面内提示条 + 全屏弹窗，弹窗内含文档卡片网格和同意/拒绝按钮）
- 提交按钮
- 「或继续使用」分隔线
- Passkey 登录按钮
- OAuth 按钮组：GitHub / Google（单个时全宽，两个时并排）、LinuxDo、钉钉、微信、OIDC，各带品牌标识
- 底部：注册链接

【两步验证弹窗】
居中弹窗：圆形盾牌图标 + 标题 + 说明 + 打码邮箱 + 6 个独立数字输入格（支持粘贴和退格跳格）+ 取消按钮。满 6 位自动提交。

【注册页】
- 邮箱 / 密码（含强度提示）
- 邀请码（必填时）：带实时校验的三态指示器（校验中转圈 / 通过绿勾 / 失败红叉），通过后下方展开成功提示条
- 推广码（选填）
- 优惠码（选填）：同样实时校验，成功时显示赠送金额
- 人机验证 + 协议
- 提交按钮
- OAuth 按钮组
- 注册关闭时：整个表单替换为说明卡片

【邮箱验证页】
验证码输入（单个宽输入框，居中大号等宽字体带字距）+ 已发送提示条 + 提交按钮 + 重发区（含倒计时）

亮色与暗色两套。
```

## 其余页面（简要）

```
以下页面请沿用已建立的设计系统，按相同的信息层次逻辑设计：

【我的订单 /orders】
筛选卡（状态下拉 + 刷新 + 返回充值按钮）+ 订单表格（订单ID/订单号/支付金额含手续费说明/支付方式/状态徽章/创建时间/操作）+ 分页。行操作含「取消」（待支付时）和「申请退款」（已完成且渠道支持时）。配两个弹窗：取消确认、退款申请（含必填的退款原因文本域）。

【兑换码 /redeem】
余额横幅卡（渐变背景 + 大号余额 + 并发数）+ 兑换表单卡（大号输入框 + 提交按钮）+ 结果卡（成功为绿色系含明细，失败为红色系）+ 说明卡（编号规则列表）+ 最近活动列表（类型图标 + 标题 + 时间 + 正负着色金额 + 码前缀）。

【可用渠道 /available-channels】
搜索框 + 刷新，主体是一张纵向合并单元格的表格：渠道名和描述纵向合并，右侧展开平台/分组/支持模型。专属分组用盾牌标记排在前，公开分组在后。

【推广返利 /affiliate】
四张统计卡（返利比例/邀请人数/可用额度/累计额度）+ 邀请卡（两栏：邀请码和邀请链接，各带复制按钮）+ 使用说明提示框 + 转账卡（转入余额按钮）+ 被邀请人表格。

【批量图片生成 /batch-image】
筛选行（任务名搜索/API Key/状态/下载状态）+ 操作行（重置/刷新/使用指南/创建任务）+ 批量操作条（勾选后出现）+ 父子行表格（可展开子任务，含勾选列、任务名、模型、Key、状态、成功失败计数、花费、下载状态、操作）。配四个弹窗：任务详情（含明细表和图片缩略图预览）、图片大图预览、创建任务（含 Prompt 构建器：文本域 + Custom ID + 输出数量 + 参考图上传 + 已添加列表）、使用指南。

【自定义页面 /custom/:id】
两种模式：Markdown 模式需要左侧可收起的目录侧栏（按标题层级缩进，当前阅读位置高亮）+ 右侧内容区；iframe 模式需要右上角浮动的「新标签页打开」按钮 + 全高嵌入区。

【支付结果页 /payment/result】
居中卡片，三种状态：成功（绿色勾）/ 处理中（旋转环 + 自动刷新说明）/ 失败（红色叉）。下方订单信息卡（订单ID/订单号/充值金额/手续费/实付/到账/支付方式/状态）+ 两个按钮（返回充值 / 查看订单）。

【404 页】简洁的未找到页面，含返回首页按钮。

每页都需要亮色与暗色两套。
```

---

# 使用建议

1. **先贴主简报**，让 Claude Design 建立设计系统、落地页和控制台外壳。这一步产出的色板和组件会成为后续所有页面的基准。
2. **确认设计系统后再逐页推进**。建议顺序：仪表盘（确立数据展示语言）→ API 密钥（最复杂，验证表格与弹窗规范）→ 落地页精修 → 其余页面。
3. **`/monitor` 需要单独对待**——它有两套 UI，建议分两次提。
4. 简报里没提「简易模式」（会隐藏约一半菜单项）和「后台模式」两种运行态，等主体设计稳定后再补。

---

## Verification

设计产出后按以下清单核对：

- **无 sub2api 痕迹**：主色不含 Teal 系；首页无终端窗口；无玻璃拟态+光斑背景组合；无开源仓库链接；界面无版本号
- **功能无遗漏**：对照第一部分的 13 个功能页 + 10 个公开页清单逐项核对，特别是 `/keys` 的 6 个弹窗和 `/purchase` 的支付状态机
- **信息惯例保留**：双值成本（实际彩色 / 标准灰色删除线）、倍率对比（专属加粗 / 原倍率删除线）、配额三窗口进度条、「∞ 无限制」表达
- **双模完整**：每页亮色暗色都有，暗色不是简单反色
- **响应式**：侧边栏三态（展开/折叠/抽屉）、表格在 1024px 以下转卡片、指标卡列数自适应
- **状态齐全**：每个数据页都有加载骨架、空态、错误态
